# PLAN — word 工厂 分阶段计划、验收策略与未决问题

> 写于 2026-09-21。**本文件里的未决问题需要用户裁决后才动代码**（§5）。

## 1. 目标（一句话）

把"一个 Word 文档从毛坯到规范"这件事，做成**一批文件一次跑完**的外置工具：
不装宏、不开 Word、结果可复现、每个文件都有改动报告。

## 2. 技术底座（已验证可行，无第三方依赖）

`.docx` 就是一个 zip，里面是 OOXML。标准库两件套就够：

- `zipfile` —— 打开/重打包；**只重写改动过的部件，其余部件按原字节搬过去**（保真）。
- `xml.etree.ElementTree` —— 读写 `word/document.xml`、`word/styles.xml`、`word/numbering.xml`、
  `word/header*.xml`、`word/settings.xml` 等。

**唯一真正的难点是"文本层"**：Word 会把一句话按格式变化切成好几个 run（`w:r`），
`w:t` 里还会被拆开（拼写检查、修订、lang 标记都会造成拆分）。
所以不能对 XML 做朴素字符串替换，必须先建 **逻辑文本 ↔ run 序列的映射**，改完再回写。
这一层做对了，后面每个宏都只是"在逻辑文本上做一次变换 + 把属性写回对应 run"。

## 3. 阶段（每阶段都要能独立验收）

| 阶段 | 内容 | 完成标志（可测） |
|---|---|---|
| **M1** | OOXML 内核：`Package`（打开/保存/部件级保真）、`text_layer`（逻辑文本 ↔ run）、`report`（改动计数） | 单测：对同一 `.docx` 打开→保存，**除目标部件外所有部件字节不变**；跑两次结果字节一致（幂等） |
| **M2** | 第一批宏：`格式规范化`（全文黑/去高亮/字体）、`去无意义空格`、`特殊字符替换` | 差分测试：与对应 VBA 宏在同一输入上的输出**逐段等价**（§4） |
| **M3** | `表头格式`（段首两格 + `表XX-X` + 空格居中）、`段落配方生成`/`段落重配`（读写 `.xlsx`） | 配方能从 Word 宏生成的 xlsx **读**；我们生成的 xlsx 能被 Word 宏**读**（双向互通） |
| **M4** | 配方编排：勾选 + 排序 + 一键批量 + 每文件报告；CLI + GUI | 批 10 个文件：顺序正确、失败不中断、报告逐项可追 |
| **M5** | PDF 导出（编排外部渲染器）、可选后端探测（Word/WPS/LibreOffice） | 装了哪个用哪个；没装给出人话提示；失败可重试 |

> 状态注（2026-09-26）：**M1–M5 全部落地**（305 条单测全绿）。M4 的 GUI 是本地 Web 界面，
> M5 是渲染器编排（Word/WPS/LibreOffice，页数从 PDF 自己数）。剩下的只有 §6 的页码 /
> 目录外观，按用户意见**押后**。

## 4. 验收策略（沿用上一轮被证明有效的做法）

**核心武器：差分测试**。这台机器上有 Word，那 12 个 VBA 宏原件也在——所以每实现一个宏，就：

1. 造一个固定的夹具 `.docx`（含该宏要处理的全部花样：多 run 段落、合并单元格、页眉、高亮、上下标…）；
2. **用 Word + 对应 VBA 宏**跑一份结果（这是"金标准"）；
3. **用我们的 XML 宏**跑同一个输入；
4. 比对：正文逻辑文本、关键格式属性（字体/颜色/高亮/缩进/边框/上下标）、后续宏能否继续处理；
5. 把"哪些属性没对齐"逐条列出来，**不设"差不多就行"**。

另外两条硬性纪律（上一轮学到的）：

- **幂等**：同一个宏跑两次，第二次什么都不改（有测试）；
- **保真**：不许顺手改到别的部件（有测试：除目标部件外字节不变）。

以及一条边界测试：**真 `Normal.dotm` 与本工具的输入文件都要留指纹**（sha256），
跑完比对——工具不许碰它没被要求碰的东西。

## 5. 未决问题（**需要用户裁决**）

> 状态注（2026-09-21）：第 1–4 条已由用户当场裁决，保留原文做记录。

1. **第一版范围**：我建议 M1+M2（内核 + 格式规范化/去空格/特殊字符替换）先落地，
   因为它们最常用、最不容易踩坑；表头与配方（M3）随后。
   ——要不要按这个顺序？还是先做你最缺的那个？
   **【已裁决】用户 2026-09-21：「1\行」** —— 顺序按 M1 → M2 → M3 走；实际执行时用户又说「开做 M3」，
   于是题注统一（M3a）先落地了，段落配方（M3b）紧随其后。
2. **形态**：这个工具天然是"批量 + CLI"，但你说过要"选定 + 排序 + 一键"。
   我建议 **CLI 为主 + 一个薄 GUI**（选文件/勾选宏/排序/看报告）。
   ——GUI 要不要第一版就做？还是先 CLI，你摸顺了再加界面？
   **【已裁决】用户 2026-09-21：「2\要GUI版本」。**
3. **"选定内容"怎么表达**：VBA 宏里很多功能依赖"当前选中了什么"（如 `段落配方生成器` 选中高亮文字）。
   外置工具没有"选区"，所以要么**改由规则指定**（例如"处理所有高亮文字"、"处理标题1样式"），
   要么**在文档里留锚**（如自定义样式/批注）。——你更接受哪一种？
   （我倾向第一种：规则化反而更稳定、可复现。）
   **【已裁决】用户 2026-09-21：「3\接受」** —— 即接受**规则化**（不做选区）。
4. **表头居中的"空格算法"**：需要知道你的**成文规矩**——
   `表XX-X` 与表名之间的空格数按什么算？（固定总宽？按字号？按纸张宽度？）
   参考宏里应该有实现，我已让子代理去挖（`docs/REFERENCE-MACROS.md` §4）。
   ——挖出来之后我给你确认，还是你直接口述规矩？
   **【已裁决】用户 2026-09-21：「按规则 B 重算可以，表X-Y（不带空格）可以，表格要顺手居中 开做M3」**
   —— 规则 B = 名字中点落在「编号之后 → 版心右边界」的中点（已从 12 个真实表题反推并验证）；
   图题另有一套：**整段居中 + 编号与名字之间正好 1 个空格**（用户同日单独提出）。
5. **PDF 导出**：本机有没有装 LibreOffice？没有的话，PDF 只能走 Word/WPS。
   ——允许我们调用 Word/WPS 来出 PDF 吗？（这不违反"不依赖 Word 改文档"，只是借用它渲染。）
   **【仍未裁决】**
6. **字体规则的"不合格"口径**（2026-09-21 新增，**已裁决**）：正式版要求"通体黑色、没有不合格的字体"——
   "通体黑"是明确的（所有非黑颜色改 `000000`），但**哪些字体算不合格是业务规矩**，只能你定。
   **【已裁决】用户 2026-09-21 原话：「西文统一为 Times new roman，中文常见的几种基本都是宋体、仿宋GB2312、黑体、楷体」。**
   → 落到 `rules/fonts.json`：keep = 宋体/黑体/楷体/Times New Roman；replace = 仿宋→宋体、仿宋_GB2312→宋体；
   `default`（其余中文）= 宋体；`default_latin`（其余西文）= Times New Roman；`default_scope = "all"`；
   符号字体永不碰。（用户先前的旧宏依据 `规划报告一键宏.bas:71` 与此一致。）
   实测（他这份文档）：仿宋 → 宋体 1729 处、Arial → Times New Roman 18 处、Tahoma → Times New Roman 12 处；
   正式版输出 `AUDIT=PASS`。

7. **目录（TOC）的样子**（2026-09-21 新增，**明确押后**）：用户说「产物的目录十分丑，目录的功能我们需要
   后期项目大体完成做开发，可以先继续进行其它的内容」。
   → 现状事实（留证）：他文档里 36 个目录段落的字体是从**段落样式 → `docDefaults`** 继承来的
   （`toc 1`/`toc 2` → `Normal` 上写的是 `w:eastAsiaTheme="minorEastAsia"`，而主题里那个槽位是**空的**），
   所以正文换字体**不影响**这些目录段落；真正决定目录观感的是段落样式与制表位/前导符，
   以及"页码是排版结果"这件事（见 §6）。**本轮不碰，等主体功能做完再单独做。**

8. **`recipe gen`（生成端）的两条待裁**（2026-09-21 新增，**2026-09-22 已由用户裁决/验证**）：

   ① **"高亮"作用范围** → 取**整篇正文**（已存在的配方区间自动跳过）。
   ② **"隐形修正"要不要照抄**（`段落配方生成器.bas:177-213`：高亮→非高亮 时把变量范围
   去掉最后一个字符）→ **不照抄**（`--trim-last-char` 可开）。
   **用户 2026-09-22 明确：「原本的段落配方有偏移是宏的缺陷」** —— 与我们的判断一致
   （XML 里高亮状态是精确的，多砍一个字符会把 `3.5` 存成 `3.`）。

   同日用户给了两份真实产物做金标准（`段落重配.docx` + `数据表.xlsx`），比对结论见
   **`docs/GOLD-STANDARD.md`**：我们的读端读得进宏的 xlsx 与配方，**重建结果与宏自己写的
   39 个内容段落逐字一致**。比对还揪出两个真问题（"取最后一份配方"会让解析失败；
   变量数对不上不该报错）——都已修，回归测试已钉。
9. **`tableclean`（去无意义空格）的档位与什么才算无意义**（2026-09-22 新增，**待用户裁决**）：
   命令已按参考宏的三档实现（`1` 仅空格 / `2` 仅回车 / `3` 两者，默认 3）。
   **但实测提醒**：在用户自己的报告上，档位 1 命中 **0** 格，档位 2/3 命中 **9** 格，
   而这 9 格**全是故意折行的表头**（`防洪标↵准`、`大坝右侧/开敞↵式`、`实测库容↵（万m³）`…，↵ = 换行）——
   档位 2/3 会把它们拉成一行，很可能不是用户想要的。

   → 要问的是：**无意义的口径**。两种可选做法：
   ① **保守规则**（我倾向）：只删「首尾的空段落 / 连续的重复换行」，**保留单个内部换行**
   （那通常是故意折行）；
   ② 照宏原样（档位 2/3 全删），但**只对整格是数字的单元格**生效（数字里的换行肯定是脏数据）。
   在用户裁决前，**不对真实报告跑档位 2/3**；已把这条写进 README 的用法说明里当警告。
10. **表格款式（`tablestyle`）**（2026-09-22 用户提出，**已落地**）—— 用户追加要求：
    「保存已经调好的设置（一个调得满意的表格时间成本比较高，因此需要保存）和预览两个功能」，
    两者都做了：`capture`（把调好的表读成命名款式）+ `preview`（新建一份小文档，每种款式各渲染
    3 张代表性表格，用 Word 打开挑）。原设想留存如下 ↓：用户原话
   「有时候 word 里的表格排布很丑，我们的 XML 框架能否有某种定义表格款式的方式，让表格变得更好看」。
   **可行**：OOXML 里表格外观全是 XML —— `w:tblPr`（对齐/宽度/布局）、`w:tblBorders`（框线）、
   `w:tcMar`（单元格内边距）、`w:tcPr/w:vAlign`（垂直居中）、`w:trPr/w:tblHeader`（表头跨页重复）、
   `w:tblStyle`（命名表样式）。建议做法：再开一个外置规则文件 `rules/tablestyle.json`，
   定义若干**命名款式**（如「三线表」「全框线」「斑马纹」）＝ {框线粗细/颜色、内边距、垂直居中、
   表头处理(加粗/居中/重复)、列宽策略(等宽/按内容估/百分比)、字号}，按文档或按表格序号套用。
   已知边界：「最佳列宽」取决于渲染器，XML 层只能**按内容估算**（与 `xlsx.py` 的列宽估算同一路子），
   要跟 Word 的"自动调整"完全一致做不到 —— 这一条要跟用户说清。

## 6. 页码（单独另算，本轮不碰）

你的目标是"目录里的页码数值和正文标题对上"。这件事**必须有分页引擎**，XML 做不到。
将来可以走的四条路（先记着，等有项目思路再动）：

1. 交回 Word/WPS 更新（现状，会重置目录）；
2. **冻结目录**：当页码正确时把目录域转成静态文字（从此不再重置，但也不再自动变）；
3. 从**已渲染的 PDF** 反查每个标题的实际页码，再写回目录（需要一个 PDF 文本层解析）；
4. LibreOffice 无头渲染取分页。

## 7. 不做（重复一遍，免得后面跑偏）

- 不打包 exe（除非你要求）；不联网；不装第三方包（只用标准库）。
- 不处理 `.doc`（二进制）；不动 `.docm` 里的 VBA 工程。
- 不做排版/渲染本身（页码、PDF 像素级输出）——只做**编排**。

## 8. 写文件的纪律（从上一轮的教训里来）

1. **默认不覆盖原文件**。`Package.save()` 直接拒绝写到自己的路径；输出永远是新文件或有明确指定的目录。
   要不要原地覆盖，由调用方**显式**决定，并且默认先备份。
2. **每次运行给改动报告**：每个文件改了几处、改了哪类东西、哪些没做到。
   批量作业没有报告就等于没有可审计性。
3. **参考宏里有反面教材**：`格式规范化.bas` 跑完会**保存并关闭文档**（规格书 §2.7）——
   我们**不**这么做。工具只负责改，存哪、覆不覆盖、关不关，都是调用方的事。
4. **幂等**：同一个宏跑第二遍不该再改任何东西（有单测兜着）。
