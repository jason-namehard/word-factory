# -*- coding: utf-8 -*-
"""算子（宏）：每个"宏"= 一个具名的、可排序的修改步骤。

约定（M4 的编排会依赖它）：

* 每个算子先**算计划**（只读），再**落地**（写）；``--dry-run`` 就是把计划打印出来。
  这样"预览"和"真跑"走的是一条逻辑，不会出现"预览说改 3 处、真跑改了 5 处"。
* 算子在 ``Document`` 上工作，只动白名单内的部件（默认只有正文）。
* 报告统一形状：``{"op": 名字, "changed": 处数, "details": [...], "notes": [...]}``。
"""

__all__ = ["header"]
